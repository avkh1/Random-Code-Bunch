# Iterate through ASCII values from 0x20 (space) to 0x7E (tilde)
for char_code in range(0x20, 0x7F):
    # Print the character
    print(chr(char_code), end=' ')

# Print a newline at the end
print()
input()
